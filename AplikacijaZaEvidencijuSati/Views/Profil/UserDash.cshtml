@model AplikacijaZaEvidencijuSati.Models.UserPassword
@{
    ViewBag.Title = "UserDash";
}

@using (Html.BeginForm("UserDash", "UserDash", FormMethod.Post))
{
    <fieldset style="text-align:center;">
        <legend style="font-size:4rem;color:rgb(51, 122, 183); font-family:'Franklin Gothic'">PROFIL</legend>
        <table class="center">

            @if (Session["Ime"] != null)
            {
                <text>
                    Ime: @Session["Ime"].ToString()
                </text>
            }
            <br />
            @if (Session["Prezime"] != null)
            {
                <text>
                    Prezime: @Session["Prezime"].ToString()
                </text>
            }
            <br />
            @if (Session["Email"] != null)
            {
                <text>
                    Email: @Session["Email"].ToString()
                </text>
            }
            <br />
            <br />
            @*<tr style="align-items:center">
                <td>Old Password</td>
                <td>
                    @Html.EditorFor(pass => pass.Password)
                </td>
                <td>@Html.ValidationMessageFor(pass => pass.Password)</td>
            </tr>
            <tr class="rowspace">
                <td>New Password</td>

                <td>
                    @Html.EditorFor(pass => pass.NewPassword)
                </td>
                <td>@Html.ValidationMessageFor(pass => pass.NewPassword)</td>
            </tr>

            <tr class="rowspace">
                <td colspan="3" id="button">
                    <input type="submit" value="Change Password" />
                </td>
            </tr>*@
            <tr class="rowspace"><td colspan="3">@ViewBag.Message</td></tr>
        </table>
    </fieldset>
}

